-- 이 경기를 배당만으로 예측했는지 회차 생성 시점에 고정한다.
--
-- 왜 저장해야 하는가: 지금까지는 "리그가 모델 지원 리그인가"로 매번 다시 판단했다.
-- 그런데 지원 리그인데 팀명 매핑이 빠져 배당 기반으로 등록되는 경우가 생긴다(48회차 인테르/AC몬차).
-- 나중에 그 팀명을 NAME_MAP에 추가하면 판단이 뒤집혀서, elo_diff=0으로 저장된 경기가
-- 갑자기 "모델 예측"으로 표시된다 - 격차 0 + 홈어드밴티지라는 가짜 신호에 확신도 등급까지
-- 붙는다. 등록 시점의 사실을 그대로 남겨 그 뒤집힘을 막는다.
ALTER TABLE round_predictions ADD COLUMN market_only INTEGER NOT NULL DEFAULT 0;
